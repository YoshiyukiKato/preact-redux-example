{
  "name": "preact-redux-example-typescript",
  "version": "1.0.0",
  "description": "Typescript version of preact-redux-example",
  "scripts": {
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch"
  },
  "keywords": [
    "preact",
    "redux"
  ],
  "license": "MIT",
  "author": "Yoshiyuki Kato <yoshiyuki-kato@otuscraft.com>",
  "devDependencies": {
    "if-env": "^1.0.0",
    "less": "^2.7.3",
    "less-loader": "^4.0.5",
    "preact-cli": "^2.0.0",
    "preact-cli-plugin-typescript": "^0.2.2",
    "typescript": "^2.7.2"
  },
  "dependencies": {
    "preact": "^8.2.7",
    "preact-redux": "^2.0.3",
    "redux": "^3.7.2"
  }
}
